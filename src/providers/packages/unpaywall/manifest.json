{
  "id": "unpaywall",
  "name": "Unpaywall OA Resolver",
  "version": "1.0.0",
  "kind": "artifact_resolver",
  "entry": "provider.js",
  "description": "Resolve DOIs to ordered open-access artifact locations via the Unpaywall REST API (metadata only; no byte download).",
  "author": "Paper Search material-providers",
  "minCliVersion": "0.1.0",
  "capabilities": {
    "inputs": ["identifier"],
    "identifierSchemes": ["doi"],
    "outputs": ["locations"],
    "network": true
  },
  "configSchema": {
    "email": {
      "type": "string",
      "required": false,
      "default": "xxx@example.com",
      "env": ["UNPAYWALL_EMAIL"],
      "label": "Contact email",
      "description": "Unpaywall contact email. The placeholder keeps requests available but the CLI will recommend replacing it."
    }
  },
  "permissions": {
    "network": ["https://api.unpaywall.org/*"]
  },
  "rateLimit": {
    "requestsPerMinute": 60
  }
}
